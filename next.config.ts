import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['source.unsplash.com', 'plus.unsplash.com','placehold.co'], // Add 'plus.unsplash.com' to the list
  },
  /* config options here */
};

export default nextConfig;
